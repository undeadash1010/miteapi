PORT=8080
# Use a reachable Yattee Server instance. The default in server.js is only a fallback.
YATTEE_URL=https://your-yattee-server.example
# Optional when the upstream requires authentication.
YATTEE_USER=
YATTEE_PASS=
